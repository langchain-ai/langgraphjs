---
"@langchain/langgraph-checkpoint-mongodb": patch
---

Allow the `mongodb` driver 7.x alongside 6.21+ so applications on the current driver major can install the checkpointer without a duplicate driver copy.

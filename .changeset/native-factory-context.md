---
"@langchain/langgraph-api": minor
---

Pass run context to native Node graph factories through a second `GraphFactoryRuntime` argument. Distinguish execution from assistant inspection and thread state operations while preserving existing config-only factories.

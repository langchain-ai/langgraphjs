---
"@langchain/langgraph-supervisor": minor
---

feat(supervisor): add `addHandoffMessages` option to omit supervisor-to-agent handoff bookkeeping messages from the expert agent's message history, matching the Python package.

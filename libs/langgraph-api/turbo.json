{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["**/dist/**"]
    },
    "build:compile": {
      "dependsOn": ["^build:compile"],
      "outputs": ["**/dist/**"]
    }
  }
}
